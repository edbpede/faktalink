[install]
# Single-package project: hoisted is Bun's default here and what Astro/Vite expect.
linker = "hoisted"
# Pin what we resolve. A video index built from a live crawl is reproducible only
# if the toolchain that built it is too.
exact = true
